#include<stdio.h>
int main()
{
	int i=1,num;
	printf("enter the value of num");
	scanf("%d",&num);
	while(i<=num)
	{
		if(num%2==0)
		printf("even number=%d",i);
		else
		printf("odd number=%d",i);
		i++;
	}
	return 0;
}
